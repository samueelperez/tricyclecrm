const { createClient } = require('@supabase/supabase-js'); require('dotenv').config(); async function getProformas() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { data, error } = await supabase.from('proformas').select('*').order('fecha', { ascending: false }); if (error) console.error('Error:', error); else { console.log('Total proformas:', data.length); data.forEach(p => console.log(JSON.stringify(p, null, 2))); } } getProformas();
